---
doc_type: "permission_registry"
version: "1.1.0"
updated: "2026-05-12"
scope: "Engineer Online 5.1~5.9"
authority: "SSOT"
---

# Engineer Online — 权限码注册表（Permission Code Registry）

> **用途**：本文档是 Permission Code 的**单一真相源（SSOT）**。所有后端鉴权、前端可见性控制、审计日志必须引用本表定义的码值，不得自行新增或修改。
>
> **关联文档**：
> - 角色定义：`requirement/00-需求总览.md` §1.5
> - 鉴权技术实现：`standard/技术架构.md` §10
> - 模块业务规则：`requirement/modules/05.x-xxx.md` §5.x.4

---

## 1. 命名规范

格式：`{资源}:{动作}:{范围}`

| 段                | 取值规范                       | 示例                                                                               |
| ---------------- | -------------------------- | -------------------------------------------------------------------------------- |
| **资源（resource）** | 英文小写单词，对应核心实体名             | `question` / `answer` / `group` / `vote` / `notification` / `translate`          |
| **动作（action）**   | 英文小写动词                     | `view` / `create` / `update` / `delete` / `audit` / `accept` / `cast` / `resync` |
| **范围（scope）**    | `any`（任意对象）/ `own`（仅自己创建的） | `any` / `own`                                                                    |

**范围语义**：
- `:any`：不需要关心资源归属，直接放行
- `:own`：Service 层必须比对资源 `author_id / user_id` 与当前 `currentUserId()`，不一致则抛 `FORBIDDEN`

---

## 2. 角色简称

本文档涉及两套独立的角色体系：**App 端角色**（GAC APP 用户）和**管理后台角色**（运营管理后台操作员），不可混用。

### 2.1 App 端角色

> 对应 `requirement/00-需求总览.md` §1.5 角色定义

| 中文名  | 英文常量         | 说明                                                   |
| ---- | ------------ | ---------------------------------------------------- |
| 所有用户 | `ALL`        | 任何用户（含未登录）。未登录用户无独立角色，属于 `ALL` 范畴                    |
| 注册用户 | `REGULAR`    | 已注册登录但未绑定车辆                                          |
| 车主   | `CAR_OWNER`  | 已绑定车辆的用户                                             |
| 授权车主 | `AUTH_OWNER` | 被车主授权的使用者                                            |
| 官方号  | `OFFICIAL`   | `OFFICIAL_ENGINEER` + `OFFICIAL_PM`（两类子类型权限等价，RR-04） |
| 作者   | `AUTHOR`     | 运行时动态判定（`author_id = currentUserId`），非数据库固定角色字段      |

**说明**：
- 同一用户可同时拥有多个 App 端角色，只要匹配到圈子配置中任一角色即拥有该权限（RR-05）
- 例如：某用户既是"车主"又是某条问题的"作者"

### 2.2 管理后台角色

| 中文名 | 英文常量 | 说明 |
|---|---|---|
| 运营管理员 | `ADMIN` | 运营管理后台的系统管理员，后台指定 |

**说明**：
- `ADMIN` 不是 App 端角色，仅在运营管理后台生效
- 管理后台权限通过 Sa-Token 角色校验（`@SaCheckRole("ADMIN")`），不走 App 端的 Permission Code 体系

---

## 3. 权限码完整清单

> **Permission Code 默认值 vs 圈子配置**：
> - Permission Code 的"默认拥有的角色"是**系统级宽松默认值**（兜底），表示"哪类用户先天具备该操作的能力"
> - 实际权限需再叠加圈子配置限制（`access_roles` / `post_roles` / `reply_roles` / `follow_up_roles`），两者是 **AND** 关系
> - 例如：`answer:create:any` 默认角色为 `REGULAR`，但在线工程师圈子的 `reply_roles` 默认为 `official`，最终只有官方号能回答
> - `:own` 范围的权限不叠加圈子配置，仅做归属校验

### 3.1 圈子（Group）

| Permission Code    | 含义              | 归属端 | 默认拥有的角色 | 关联 BR        | 备注                      |
| ------------------ | --------------- | ------ | ------- | ------------ | ----------------------- |
| `group:view:any`   | 查看圈子列表/详情       | App 端  | ALL     | BR-5.2-07    | 受圈子 `access_roles` 额外限制 |
| `group:manage:any` | 创建/编辑/删除/启用停用圈子 | 管理后台 | ADMIN   | BR-5.1-01~10 | 见模块 5.1                 |

### 3.2 问题（Question）

| Permission Code       | 含义             | 归属端   | 默认拥有的角色                       | 关联 BR     | 备注                    |
| --------------------- | -------------- | ----- | ----------------------------- | --------- | --------------------- |
| `question:view:any`   | 查看任意审核通过的问题    | App 端 | ALL                           | BR-5.2-01 | 需满足"公开状态为公开"          |
| `question:view:own`   | 查看自己未通过审核的问题   | App 端 | 作者                            | BR-5.2-09 | 仅作者本人可见               |
| `question:create:any` | 发布问题           | App 端 | REGULAR | BR-5.3-01 | 受圈子 `post_roles` 叠加限制 |
| `question:delete:own` | 删除自己的问题        | App 端 | 作者                            | BR-5.4-11 | 软删除；已采纳的问题仍可删除         |
| `question:delete:any` | 删除任意问题         | 管理后台  | ADMIN                         | BR-5.7-03 | 软删除 + 审计；已采纳的问题仍可删除 |
| `question:audit:any`  | 审核问题（通过/驳回/隐藏） | 管理后台  | ADMIN                         | BR-5.7-02 | 见模块 5.7               |

### 3.3 回答与回复（Answer）

| Permission Code     | 含义              | 归属端   | 默认拥有的角色 | 关联 BR                 | 备注                               |
| ------------------- | --------------- | ----- | ------- | --------------------- | -------------------------------- |
| `answer:create:any` | 回答问题（一级回答）      | App 端 | REGULAR | BR-5.4-02             | 受圈子 `reply_roles` 叠加限制（回答权限）     |
| `answer:reply:any`  | 追问（对一级/二级回答的回复） | App 端 | REGULAR | BR-5.4-11 / BR-5.4-16 | 受圈子 `follow_up_roles` 叠加限制（追问权限） |
| `answer:delete:own` | 删除自己的回答         | App 端 | 作者      | BR-5.4-11             | 软删除；已采纳的回答也可删除                |
| `answer:delete:any` | 删除任意回答          | 管理后台  | ADMIN   | BR-5.8-04             | 软删除 + 审计；已采纳的回答也可删除                |
| `answer:audit:any`  | 审核回答            | 管理后台  | ADMIN   | BR-5.8-01             | 见模块 5.8                          |
| `answer:accept:own` | 采纳自己问题下的回答      | App 端 | 问题作者    | BR-5.4-03 / BR-5.4-12 | 仅可采纳 1 条，采纳后关闭追问入口               |
| `answer:resync:any` | UOP 同步失败重同步     | 管理后台  | ADMIN   | BR-5.8-03             | 重试已失败推送                          |

### 3.4 投票（Vote）

| Permission Code | 含义      | 归属端   | 默认拥有的角色                                     | 关联 BR     | 备注         |
| --------------- | ------- | ----- | ------------------------------------------- | --------- | ---------- |
| `vote:cast:any` | 投票（赞/踩） | App 端 | REGULAR + CAR_OWNER + AUTH_OWNER + OFFICIAL | BR-5.4-04 | 赞/踩互斥 + 防抖 |

### 3.5 通知（Notification）

| Permission Code | 含义 | 归属端 | 默认拥有的角色 | 关联 BR | 备注 |
|---|---|---|---|---|---|
| `notification:read:own` | 读取自己的通知 | App 端 | 自己 | BR-5.6-01 | 仅本人 |
| `notification:clear:own` | 标记自己的通知已读 | App 端 | 自己 | BR-5.6-02 | 单条/批量 |

### 3.6 翻译（Translate）

| Permission Code | 含义 | 归属端 | 默认拥有的角色 | 关联 BR | 备注 |
|---|---|---|---|---|---|
| `translate:invoke:any` | 调用翻译服务 | App 端 | ALL | BR-5.4-18 | 受频次限制（防刷） |

---

## 4. 角色 × 权限码 矩阵（速查）

> ✅ 拥有 / ❌ 无 / ⚠️ 条件拥有（需叠加圈子配置或资源归属）

| Permission Code          | 所有用户（含未登录） | 注册用户 | 车主用户 | 授权车主 | 官方号 | 作者  |
| ------------------------ | ---------- | ---- | ---- | ---- | --- | --- |
| `group:view:any`         | ✅          | ✅    | ✅    | ✅    | ✅   | -   |
| `question:view:any`      | ✅          | ✅    | ✅    | ✅    | ✅   | -   |
| `question:view:own`      | -          | -    | -    | -    | -   | ✅   |
| `question:create:any`    | ❌          | ❌    | ✅    | ✅    | ✅   | -   |
| `question:delete:own`    | -          | -    | -    | -    | -   | ✅   |
| `answer:create:any`      | ❌          | ❌    | ❌    | ❌    | ✅   | -   |
| `answer:reply:any`       | ❌          | ❌    | ❌    | ❌    | ✅   | ✅   |
| `answer:delete:own`      | -          | -    | -    | -    | -   | ✅   |
| `answer:accept:own`      | -          | -    | -    | -    | -   | ✅   |
| `vote:cast:any`          | ❌          | ✅    | ✅    | ✅    | ✅   | -   |
| `notification:read:own`  | -          | -    | -    | -    | -   | ✅   |
| `notification:clear:own` | -          | -    | -    | -    | -   | ✅   |
| `translate:invoke:any`   | ✅          | ✅    | ✅    | ✅    | ✅   | -   |

**说明**：
- 此矩阵描述 **"在线工程师"圈子的默认权限配置**（App 端角色）。不同圈子可通过配置覆盖（RR-01）。
- 管理后台 `ADMIN` 角色的权限（如 `question:delete:any`、`question:audit:any`、`answer:delete:any`、`answer:audit:any`、`answer:resync:any`、`group:manage:any`）不在此矩阵中列出，详见 §3 权限码清单。
- 作者列为运行时动态判定（`author_id = currentUserId`），非数据库固定角色。
- **所有用户（含未登录）**列对应 `ALL` 角色；未登录用户无独立角色，属于 `ALL` 范畴。
- 同一用户可同时拥有多个角色，只要匹配到圈子配置中任一角色即拥有该权限（RR-05）。

---

## 5. 实现约定

### 5.1 后端

```java
// Controller 注解校验
@SaCheckPermission("question:create:any")
@PostMapping("/api/v1/questions")
public Result<Long> create(@RequestBody @Valid QuestionCreateDTO dto) { ... }

// `:own` 范围必须在 Service 层补充归属校验
public void deleteOwn(Long questionId) {
    Question q = questionMapper.selectById(questionId);
    if (!q.getAuthorId().equals(currentUserId())) {
        throw new BusinessException(ErrorCode.FORBIDDEN);
    }
    // ...
}
```

**圈子级叠加校验**：当 Permission Code 通过后，Service 层需再读取圈子配置判断当前角色是否被允许：
- `question:create:any` → 校验 `Group.post_roles`
- `answer:create:any` → 校验 `Group.reply_roles`（回答权限）
- `answer:reply:any` → 校验 `Group.follow_up_roles`（追问权限）
- `group:view:any` → 校验 `Group.access_roles`

**用户圈子权限计算**：用户在不同圈子的权限为**运行时动态计算**，不持久化存储。计算逻辑为 `用户当前角色集合 ∩ 圈子配置角色集合`，结果通过 Redis 缓存（`group:permissions:{user_id}:{group_id}`，TTL 30m）。

### 5.2 前端

```vue
<!-- Vue 3 (管理后台/H5) -->
<button v-permission="['question:audit:any']">审核</button>

<!-- Flutter -->
Visibility(
  visible: AuthStore.hasPermission('answer:accept:own') && question.authorId == currentUserId,
  child: AcceptButton(),
)
```

### 5.3 代码常量生成

后端/前端统一通过 `TOOL-04` 任务（见 `tasks/任务清单.md`）从本表生成：
- Java：`PermissionCode.QUESTION_CREATE_ANY`
- Dart：`PermissionCode.questionCreateAny`
- TypeScript：`PermissionCode.QUESTION_CREATE_ANY`

编译时校验 code 是否存在于本表，避免拼写错误。

---

## 6. 变更流程

新增或修改 Permission Code 必须：

1. 在本表对应分组追加一行，填齐"默认角色/关联 BR/备注"
2. 同步更新 `requirement/00-需求总览.md` §1.5 权限矩阵
3. 同步更新对应模块 PRD 的 §5.x.4 业务规则（若引入新业务规则）
4. 更新 `CHANGELOG.md`
5. 重新运行 `TOOL-04` 生成代码常量

**禁止**：直接在代码里写裸字符串 `"question:create:any"`，必须通过生成的常量引用。

---

## 7. 统计

| 资源 | 数量 |
|---|---|
| group | 2 |
| question | 6 |
| answer | 6 |
| vote | 1 |
| notification | 2 |
| translate | 1 |
| **合计** | **18** |
